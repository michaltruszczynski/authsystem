import React from "react";

import styles from './Admin.module.scss';

const Home = () => {
   return (
      <section className={styles['container']}>
         <h1>Admin Page</h1>
      </section>
   );
};

export default Home;